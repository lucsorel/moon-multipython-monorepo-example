language: 'python'

tasks:
  # we rely completely on the pre-commit tool because we failed to write the moon's commit-msg hook
  lint-install:
    command: 'uv run pre-commit install -f'
    options:
      runInCI: false

  lint-all:
    command: 'uv run pre-commit run --all-files'
    deps:
      - '^:install'
