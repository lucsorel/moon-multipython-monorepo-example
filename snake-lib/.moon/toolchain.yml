# https://moonrepo.dev/docs/config/toolchain
$schema: 'https://moonrepo.dev/schemas/toolchain.json'

# specifies that the sub-project uses python. Moon will find the corresponding .python-version and use it
python:
  packageManager: 'uv'
  uv:
    version: '0.5.26'
